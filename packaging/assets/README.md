# DMG assets (optional)

- **`dmg-background.png`** — Optional window background for the disk image (recommended **660×440** px or similar). If missing, `make-dmg.sh` builds a DMG without a custom background (still includes an **Applications** shortcut and icon layout).

Add your branded background here and commit it when ready.
